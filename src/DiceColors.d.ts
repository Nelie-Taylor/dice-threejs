export declare class DiceColors {
  static getColorSet(name: string): {
    background: number;
    foreground: number;
  };
  static setColorSet(name: string, colors: {
    background: number;
    foreground: number;
  }): void;
} 